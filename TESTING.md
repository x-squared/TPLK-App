# Testing Integration

This repository consumes shared test tooling from `../AppTest` in test scope only.

- Use AppTest framework runners for spec and integration checks.
- Do not import AppTest from runtime code.
